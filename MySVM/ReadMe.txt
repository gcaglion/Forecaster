MySVM is the only engine that relies on external code. It uses Thorsten Joachims' SVMlight library , slightly modified to integrate upstream with MyForecast module. Additional changes have been made to ensure proper behavior when multi-threading
